{
  "compilerOptions": {
    /* Opciones de Tipos */
    "target": "es2020",                                  /* Especifica la versión de ECMAScript objetivo. */
    "module": "commonjs",                                /* Especifica el sistema de módulos. */
    "lib": ["ES2020", "DOM", "DOM.Iterable"],            /* Especifica las librerías a incluir. */
    "jsx": "react",                                      /* Soporte para JSX (React). */
    "esModuleInterop": true,                             /* Habilita la interoperabilidad con módulos CommonJS. */
    "forceConsistentCasingInFileNames": true,            /* Asegura que las referencias a archivos coincidan en mayúsculas/minúsculas. */
    "strict": true,                                      /* Habilita todas las opciones estrictas. */
    "skipLibCheck": true,                                /* Omite la verificación de tipos en los archivos de declaración (d.ts) de librerías. */
    
    /* Opciones de Salida */
    "outDir": "./dist",                                  /* Carpeta de salida (aunque Webpack la ignora en desarrollo). */
    "rootDir": "./src",
    
    /* Módulos */
    "moduleResolution": "node",
    "resolveJsonModule": true
  },
  "include": [
    "src/**/*" // CLAVE: Incluye todos los archivos TypeScript en la carpeta 'src'
  ],
  "exclude": [
    "node_modules" // Excluye la carpeta de dependencias
  ]
}
